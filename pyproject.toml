[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "strmie"
version = "0.1.0"
description = "Tool for the analysis of CAG repeat sequences"
authors = [
    { name = "Alessandro Napoli" },
    { name = "Niccolò Liorni" },
    { name = "Tommaso Mazza" }
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = ['numpy', 'pandas', 'scikit-learn', 'matplotlib', 'h5py', 'joblib', 'jinja2', 'biopython', 'seaborn', 'colorama', 'openpyxl', 'xlrd', 'scipy', 'regex']

[project.optional-dependencies]
bam = ['pysam']

[project.scripts]
strmie = "strmie.main:main"
strmie-repeat = "strmie.generic_main:main"
